# MASIL 과거 데모 구현 감사 — 2026-08-10 확인

> 이 문서는 외부 GaipStudio 작업 트리의 관측 기록이다. 현재 팀 Q&A의 상품 정본이 아니며 기본 검색·답변에서는 제외한다. 현재 월별 결과, 연간 점수·할인, 안 8/밖 12 후보식은 `knowledge/product_model.yaml`과 `knowledge/official_positions.yaml`을 사용한다.

이 문서는 YAML을 읽지 않아도 현재 실행 화면의 점수·Care·할인 계산을 이해할 수 있도록 만든 팀 검토용 설명이다.

## 확인한 실행 기준

- 소스 checkout: `/Users/gimdonghyeon/Desktop/seniorcareservice`
- Git branch: `review/korean-dashboard-20260524`
- HEAD: `4029a37`
- 상태: **working tree에 미커밋 변경이 있음**. 아래 내용은 커밋된 릴리스가 아니라 2026-08-10 현재 작업 파일의 실제 동작이다.
- 현재 화면 진입점: `src/react-demo/main.tsx` → `GaipStudio`
- 화면 계산: `src/react-demo/gaip-decision.ts`
- 기본 데이터 생성: `src/gaip_simulation/engine.py` → `data/fixtures/gaip_simulation_bundle.json`

확인 당시 파일 SHA-256:

- `main.tsx`: `3417f2e7dd6bbcce124479d497724f11b56144a254937295ecedd48ba3b72449`
- `gaip-decision.ts`: `29583645f4d805d69fbae998fac3630975021e35f2ce8baa7590a59d5433bccb`
- `gaip_simulation/engine.py`: `b9e90b69480fbb32313737437cc775d2bdee9628fb03fc7214911ea563246323`
- `gaip_simulation_bundle.json`: `b2912c82968fcbab2e181609c05f730ab0218d04b209406d12f1e454427a62c6`

이 해시는 다른 작업이 섞인 dirty checkout의 관측 지점을 고정하기 위한 값이다. 파일이 바뀌면 구현 감사를 다시 해야 한다.

## 1. 월별 점수

매달 다음 네 점수를 가중평균한다.

- 주행거리 30%
- 생활권 안 안전 30%
- 생활권 밖 안전 20%
- 패턴 안정성 20%

관측되지 않은 축은 억지로 0점을 넣지 않고, 관측된 축의 가중치만 다시 100%로 맞춘다. 생활권이 없거나 데이터 사용률이 80% 미만이면 Hold다.

월 통합점수가 75점 이상이면 그 달은 Favorable 조건을 충족한다. 그렇지 않으면 Standard다.

팀이 발표·Q&A에서 사용할 현재 후보식은 다음과 같다.

- 월 주행거리를 연환산해 `100 / 85 / 70 / 50 / 30`점 구간에 넣는다.
- 생활권 안 안전점수는 `max(0, 100 - 생활권 안 100km당 위험 이벤트 × 8)`이다.
- 생활권 밖 안전점수는 `max(0, 100 - 생활권 밖 100km당 위험 이벤트 × 12)`이다.
- 생활권 밖이라는 위치 자체에는 벌점이 없다. 밖에서 실제로 발생한 위험 이벤트만 더 민감하게 반영한다.
- `8`과 `12`는 팀이 채택한 현재 설계 후보값이며, 문헌에서 직접 도출된 보험 요율 상수가 아니다. 최종값은 파일럿에서 재보정한다.
- 패턴 안정성은 현재 엔진에서 `100 - 위험행동 변화 × 100`으로 계산한다.
- 별도의 단일 `Pattern Change Risk` 필드는 현재 실행 경로에 없다.

### 현재 외부 데모와의 차이

2026-08-10에 감사한 외부 합성 엔진은 안·밖 모두 `×8`을 사용한다. 따라서 위 `8/12`는 현재 팀이 채택한 상품 후보식이지만 아직 그 외부 데모 코드와 기존 합성 번들에는 반영되지 않았다. 기존 Edward·Frank 점수, 180개 시나리오 결과와 대시보드 수치는 `8/8` 실행 결과로 보존하며, `8/12` 결과라고 설명하지 않는다. 외부 코드를 바꾼 뒤에는 합성 번들·대시보드 수치·회귀 테스트를 함께 다시 생성해야 한다.

## 2. 연간 점수와 Favorable

- 화면에 보여주는 연간 통합점수는 판정 가능한 월별 통합점수의 단순 평균이다.
- 연간 Favorable 자격은 평균점수 75점으로 정하지 않는다.
- 월 통합점수가 75점 이상인 달이 12개월 중 9개월 이상이면 연간 Favorable이다.
- 판정 가능한 달 자체가 9개월보다 적으면 Hold다.

즉, **연간 평균점수는 설명용 숫자이고, 추가 할인 자격은 Favorable 월 수로 결정**된다.

## 3. Care

Care는 점수 등급을 덮어쓰는 누적 상태가 아니다. 매달 다음 두 변화가 같은 달에 함께 있을 때만 해당 월에 Care review가 표시된다.

- 이동 맥락 변화 25%p 이상
- 위험행동 변화 20%p 이상

다음 달에는 다음 달 데이터로 같은 계산을 다시 한다. 이전 달 Care가 자동으로 이어지지 않는다. 연간 화면에서 `Care 1개월`처럼 횟수를 남길 수는 있지만, 이는 발생 이력이지 계속 열린 건강관리 책임이나 지속 등급이 아니다.

점수 조건과 Care가 같은 달에 함께 성립할 수 있다. 구현은 이를 `Reward + Care Review`로 보존하므로 둘 중 하나를 없애는 우선순위 규칙이 필요하지 않다.

계속 점수가 낮지만 직전 2개월 대비 동시 급변이 없다면 Standard다. Care는 낮은 절대 점수의 다른 이름이 아니라 변화 시점을 알리는 신호다. Care로 표시된 달도 월 통합점수가 75점 이상이면 연간 Favorable 월 수에 포함한다.

## 4. 현재 할인율 구현

현재 샌드박스는 다음처럼 계산한다.

1. 연간 주행거리와 차량 종류로 기존 한국 마일리지 할인율을 조회한다.
2. 연간 Favorable이면 3%p를 추가한다.
3. 최종 후보 할인율은 45%를 넘지 않는다.
4. Standard, Care, Hold에는 추가 Favorable 할인 3%p가 붙지 않는다.
5. Care 때문에 기존 할인을 차감하거나 할증하지 않는다.

```text
기본 할인율 = 기존 마일리지 할인표(연간 거리, 차량 종류)
추가 할인 = 연간 Favorable이면 3%p, 아니면 0%p
MASIL 후보 할인율 = min(45%, 기본 할인율 + 추가 할인)
후보 연 보험료 = 기준 보험료 × (1 - 후보 할인율 / 100)
```

3%p와 45%는 현재 구현된 **illustrative economics sandbox 후보값**이다. 실제 보험사의 최종 요율로 검증된 값은 아니다.

## 5. 파라미터 상태를 말하는 법

- `product_rule`: 월별 세 등급, Hold의 데이터 상태, 다음 달 재계산, Care의 직접 가격 영향 없음처럼 팀이 채택한 상품 원칙
- `current_sandbox_parameter`: 30/30/20/20, 안·밖 위험 이벤트 계수 8/8, 80% 커버리지, Care +25%p AND +20%p, +3%p·45% 상한처럼 현재 외부 코드에 있지만 재보정할 후보값
- `planned_not_implemented`: 안·밖 위험 이벤트 계수 8/12와 매달 직전 2개월 rolling 생활권처럼 팀이 채택했지만 현재 외부 합성 엔진에는 아직 반영되지 않은 설계
- `validated_result`: 180개 합성 시나리오와 테스트에서 범위를 명시해 확인한 결과

## 6. 현재 실행 경로가 아닌 옛 파일

아래 파일도 저장소에 남아 있지만 현재 `GaipStudio` 진입점의 계산 경로는 아니다.

- `src/product/annual_scoring_engine.py`: 연간 집계값으로 별도 점수와 Care를 다시 계산
- `src/product/annual_ab_comparison.py`: Care 할인 축소와 점수별 가감이 남은 과거 A/B 계산

이 두 파일을 현재 상품 규칙의 근거로 사용하면 Care 가격 분리 원칙과 충돌한다. 현재 설명은 `GaipStudio`·`gaip-decision.ts`·수정된 `gaip_simulation/engine.py` 흐름을 사용한다.

## 7. 상품 계약과 현재 합성 엔진의 차이

- 상품 계약의 거점 탐지 시작값은 `ε=260m`이고 생활권 반경은 거점별 `max(500m, min(P90, 2km))`다.
- 현재 합성 엔진은 환경 생성 축척으로 `180/420/950m`를 사용한다. 이를 실제 고객의 지역별 상품 기준으로 말하면 안 된다.
- 현재 합성 엔진은 목적지 방문 이벤트를 군집화하고 최대 2개 허브를 남긴다. 실제 상품이 출발지까지 군집화하거나 허브를 영구히 2개로 제한한다고 확정한 것은 아니다.
- 상품 계약은 해당 월을 제외한 직전 2개월로 생활권을 매달 다시 만드는 rolling 방식이다.
- 현재 외부 합성 엔진 번들은 `2025-11`, `2025-12`를 고정 기준선으로 사용한다. 코드는 이번 MCP 작업에서 바꾸지 않고 `planned_not_implemented` 차이로 공개한다.

## 8. 확인 결과

- 프런트엔드 결정·보안 테스트: 16개 통과
- Python 합성 시뮬레이션 테스트: 26개 통과
- 테스트가 확인한 내용:
  - Favorable은 동일한 월별 점수식으로 9/12개월을 센다.
  - Care는 같은 달 두 변화의 AND 조건이다.
  - Favorable과 Care는 함께 성립할 수 있고 Favorable 추가 할인은 유지된다.
  - 데이터 부족은 불이익 없는 Hold다.

## 현재 결정으로 닫히는 항목

- 점수와 Care 동시 성립 표시: 점수 + 해당 월 Care로 함께 보존 — 별도 우선순위 불필요
- Care 지속 규칙: 해당 월만 표시하고 다음 달 다시 계산
- 기본 할인율: 연간 점수 매핑이 아니라 기존 마일리지 할인표
- Favorable 추가 할인: 현재 샌드박스는 고정 3%p, 상한 45%
